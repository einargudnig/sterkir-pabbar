{
  "name": "sterkir-pabbar-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "schema:extract": "sanity schema extract --enforce-required-fields --force",
    "typegen": "sanity schema extract --enforce-required-fields --force && sanity typegen generate"
  },
  "dependencies": {
    "@sanity/vision": "^6.9.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "sanity": "^6.9.0",
    "styled-components": "^6.1.19"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
