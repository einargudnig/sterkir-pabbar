node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.env.*.local
.astro/
.output/

# Agent skills — vendored source (~3M), reinstallable from skills-lock.json,
# which IS tracked. Ignore the payload, keep the pin.
.agents/
.claude/skills/
# ...but keep the critique history: it is small, and a future `critique` run
# reads it to show whether the design is getting better or worse over time.
.impeccable/*
!.impeccable/critique/
.vercel

# Generated by `sanity build` / `sanity dev`.
studio/.sanity/

# Members' area (innri/) build output and generated route types.
innri/build/
innri/.react-router/
innri/coverage/

# Generated by `bun run types:sanity` from studio/schemas. Committed on purpose
# so a fresh clone typechecks without Sanity credentials.
!innri/app/lib/sanity.types.ts
